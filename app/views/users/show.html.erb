<div class="user-profile" id="<%= @user.id %>">

	<!-- TODO refactor user info to partial -->
	<%= render @user %>
	<%= render partial: 'follow_form', locals: {user: @user} %>
  <%= render 'shared/stats' %>
</div>
<div id="render-experiences">
	<% if @user.experiences %>
		<%= render @user.experiences %>
	<% end %>
</div>