<div class="single-dish" ><li>
  <%= dish.title %>
  <ul>
    <li>Venue: <%= dish.venue.name %></li>
      <li>
        <div class="reveal"> 
          <%= (dish.dishpicture.file? ? image_tag(dish.dishpicture.url(:thumb)) : image_tag("No_image_available.svg", size: '150x150')) %>
          <div class="hidden">
            <div class="caption">
              <div><%= dish.title %></div>    
                <div class="bottom"><%= dish.review %></div>
            </div><!-- end of .caption -->
          </div><!-- end of .hidden -->
        </div><!-- end of .reveal -->
      </li>
    <li>Review: <%= dish.review %></li>
  </ul>
</li>
</div>